<header class="browser-header">
  <ul class="nav-browser">
    <li class="h-8">
      <img
        src="/images/logo.png"
        alt="logo"
        class="h-full w-full object-cover"
      />
    </li>
    <li><a href="/browser">Trang chủ</a></li>
    <li><a href="#">Phim T.hình</a></li>
    <li><a href="#">Mới & phổ biến</a></li>
    <li><a href="/favorite">Danh sách của tôi</a></li>
  </ul>
  <div class="icon-right-browser">
    <div class="search-browser">
      <a href="#"><i class="fa-solid fa-magnifying-glass"></i></a>
    </div>
    <div class="notification-browser">
      <a href="#"><i class="fa-regular fa-bell"></i></a>
    </div>
    <div class="avatar-user-browser">
      <img src="<%= main.avatar != "" ? main.avatar : "/images/meomeo.jpg" %>"
      alt="avatar" />
      <ul class="user-option-dropdown">
        <% main.users.forEach((user) => { %>
        <li class="changeUser" data-id="<%= user._id %>">
          <form>
            <div class="img-otherUser">
              <img src="<%= user.avatar != "" ? user.avatar :
              "/images/meomeo.jpg" %>" alt="<%= "element.ur_name" %>" />
            </div>
            <span><%= user.username %></span>
          </form>
        </li>
        <% }) %>
        <li>
          <a href="/profile"><i class="fa-regular fa-user"></i>Profile</a>
        </li>
        <li>
          <a href="/log-out"
            ><i class="fa-solid fa-arrow-right-from-bracket"></i>Log out</a
          >
        </li>
      </ul>
    </div>
  </div>
</header>
<script>
  //* scroll header
  window.addEventListener("scroll", () => {
    const header = document.querySelector(".browser-header");
    if (window.scrollY > 0) {
      header.classList.add("scrollHeader");
    } else {
      header.classList.remove("scrollHeader");
    }
  });

  document.querySelectorAll(".changeUser").forEach((user) => {
    user.addEventListener("click", (e) => {
      const userId = user.getAttribute("data-id");

      document.cookie = `main=${userId}`;

      location.reload();
    });
  });
</script>
